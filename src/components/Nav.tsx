const Nav = () => {
  // TODO: Add necessary code to display the navigation bar and link between the pages. //home should link to candidate search onClick
  return (
  <div>
    <nav>
      <ul>
       <a href="/">Home</a> 
       <a href="/SavedCandidates">Potential Candidates</a>
      </ul>
    </nav>
  </div>
  );
};

export default Nav;
