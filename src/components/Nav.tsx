const Nav = () => {
  // display the navigation bar and link between the pages.
  return (
    <div>
      <nav>
        <ul>
          <a href="/">Home</a>
          <a href="/SavedCandidates">Potential Candidates</a>
        </ul>
      </nav>
    </div>
  );
};

export default Nav;
